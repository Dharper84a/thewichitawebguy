import { SiteHeader, SiteTitle } from "./styles";

const Header = (props) => {

    return(
        <SiteHeader>
            <SiteTitle>
                The Wichita Web Guy
            </SiteTitle>
        </SiteHeader>
    )
}

export default Header;